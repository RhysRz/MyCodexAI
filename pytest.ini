[pytest]
# The per-user workspace may contain imported copies of this repository.
# Limit discovery to the source test suite so pytest never collects a user's
# project as a second set of identically named test modules.
testpaths = tests
norecursedirs = workspace venv .mycodexai
addopts = -ra
